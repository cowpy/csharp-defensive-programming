# 90-archive 目录

已完成的变更方案归档于此（从 `docs/80-changes/` 移入）。

- 冷库：AI 不主动加载，仅排查「当初为什么这么改」时人工翻阅。
- 归档前确认：变更的 delta 要点已合并进对应 `docs/70-modules/*.md`，代码与文档已同一 commit 提交。
- 归档文件命名保留 changes 原名（`<yyyyMMdd>-<主题>.md`），不删除历史方案。