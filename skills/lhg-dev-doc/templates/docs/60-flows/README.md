# flows 目录（端到端业务流程）

> 放跨模块的**端到端业务流程/时序**文档，篇幅大，仅在改对应流程时整份加载。

## 定位
- 与 `70-modules/`（单个模块的职责/契约/状态机，≤200 行）互补：flows 描述**一条业务如何串起多个模块**。
- 内容：参与者、触发入口、关键时序、状态流转、异常/补偿路径、跨模块契约点。

## 命名
`<业务域>-<序号>-<流程名>.md`，如 `付款流程-04-ATS新流程.md`。

## 当前流程清单

| 文档 | 覆盖流程 |
|---|---|
| （暂无，init 时按实际业务补） | |