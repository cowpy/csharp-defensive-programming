# 代码目录 ↔ 模块文档 映射地图

> 作用：① 任务入口索引（AI 先读本表命中模块，再只读对应文档）；② 文档同步抓手（改哪块代码 → 更新哪份文档）。
> 规则：新增模块文档/代码目录必须在此表加行；「文档」列为空或标「未建」表示该模块尚未建文档，第一次改动时补建。
> init 时：按仓库真实目录结构填写，禁止保留本模板示例行。

## 业务模块（docs/70-modules/）

| 模块 | 代码位置 | 模块文档 | 一句话职责（索引关键词） |
|---|---|---|---|
| （示例）订单模块 | `src/Order/`（Service/Controllers） | order.md（未建） | 下单、状态流转、对外契约 |

## 业务流程文档（docs/60-flows/，跨模块端到端流程，按需整份阅读）

> 与 70-modules/ 的区别：70-modules/ 是**模块结构镜像**（职责/契约/坑，≤200 行）；流程文档是**端到端时序/状态流转**，篇幅大，仅在改对应流程时整份加载。

| 文档 | 覆盖流程 |
|---|---|
| （暂无） | |

## 平台与框架（通用基础设施，不建模块文档，改动需谨慎评估）

| 目录 | 职责 |
|---|---|
| （示例）`src/Infrastructure/` | 框架核心：DB 上下文、缓存、日志、通用扩展 |

## 文档目录自身

| 路径 | 内容 |
|---|---|
| `project_rules.md`（仓库根目录） | 薄宪法：加载链、同步铁律、改动分级、技术约束 |
| `docs/00-README.md` | 文档体系导航（目录结构/读哪份/写哪份） |
| `docs/10-glossary.md` | 术语表（项目特有缩写/状态码/易混命名） |
| `docs/30-requirements/` | 原始需求＆外部文档索引（锁"哪份最新"） |
| `docs/40-analysis/` | 分析产物（临时，用后 merge 或删） |
| `docs/50-summaries/` | 结项总结（一文读懂快照） |
| `docs/60-flows/` | 端到端业务流程 |
| `docs/70-modules/` | 模块镜像文档（懒生成，≤200 行/份） |
| `docs/80-changes/` | 进行中的变更方案（一人一文件）；`_template.md` 为模板 |
| `docs/90-archive/` | 已完成变更归档（冷库，不主动加载） |
| `docs/99-personal/` | 个人草稿（默认不参与分析） |