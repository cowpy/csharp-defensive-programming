# personal 目录（个人草稿 · 默认不参与分析）

> 放个人未定型的探索性笔记、临时验证、个人笔记。**进 git 正常提交，但默认不参与 AI 分析。**

## 规则
- 按人分目录：`99-personal/lhg/`、`99-personal/<同事名>/`。
- **默认不读**：project_rules 加载链不含本目录，directory-map/README 不索引它。
- **主动指定才读**：仅当你明确说「分析 docs/99-personal/lhg/xxx.md」时才加载。
- 一旦草稿开始影响公共代码/文档 → 升格到 `40-analysis/`，再固化进 `70-modules/`。