# 需求文档索引（原始需求＆外部输入）

> 本目录只放「外部给我们的」原始需求/接口文档/交接文档的**登记索引**，二进制原件（docx/xlsx/pdf）继续留在其原始位置，这里用表格锁定「哪份最新、被谁替代」——解决"接口文档一堆、不知道以哪份为准"的痛点。

## 状态标记
- ✅ **最新/权威**：项目实际实现以此为准
- ⚠️ **过时**：部分章节已被后续文档替代，勿直接照搬
- 📄 **参考**：只作背景，不作实现依据

## 当前需求文档清单

| 文档 | 位置 | 状态 |
|---|---|---|
| （init 时登记第一份外部需求文档；无则留空） | | |

## 规则
- 新增外部需求文档：登记到本表并标状态；若 supersede 旧文档，把旧文档标 ⚠️ 并注明"被 X 替代"。
- 二进制原始文档若后续要统一归档，可移入 `docs/30-requirements/raw/`，但必须同步更新本表与发版脚本引用。
- 接口字段清单以 ✅ 权威文档为准；模块文档（`70-modules/`）不重复抄字段，只引用这里。