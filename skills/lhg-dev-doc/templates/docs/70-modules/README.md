# 70-modules 目录

模块镜像文档放这里，命名与 `docs/11-directory-map.md` 的「模块文档」列一致（如 `order.md`）。

规则：
- 懒生成：没有文档的模块，第一次改动时补建，并在 11-directory-map.md 去掉「未建」标记。
- 每份 ≤ 200 行，只写：职责边界、对外接口/契约、状态机、跨模块依赖、决策原因、已知坑。
- 不抄字段清单/方法签名（代码里有）。
- 新建模块文档时复制同目录 `_template.md` 为 `<模块名>.md`，按模板段落填写。