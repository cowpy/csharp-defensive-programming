# <模块名> 模块文档

> 代码位置：<目录/关键文件>
> 只写代码读不出来的东西：职责边界、契约、状态机、决策原因、坑。
> 行数预算 ≤ 200 行，超限按代码子目录拆分（拆分边界 = 代码目录边界）。

## 职责

<这个模块管什么、不管什么（边界尤其重要；与相邻模块的职责分界线）>

## 对外契约

<对外提供的接口/服务/事件、入参出参语义、错误码约定、超时/重试约定；
不抄字段清单与方法签名（代码里有，抄了必过期）>

## 状态机与关键流程

<状态枚举穷举（代码/实体注释/脚本注释三处必须一致）、状态流转、关键时序；
可重试中间态必须配套最大重试/回收路径，终态与死信/告警路径写清楚>

## 跨模块依赖

<依赖谁、谁依赖我、契约是什么；回调/异步链路的上下游>

## 决策与坑

<为什么这么做的历史决策、踩过的事故与教训；
新决策追加到这里并带日期，不覆盖旧记录；
项目特有限制标注清楚适用范围，不当作通用结论>
